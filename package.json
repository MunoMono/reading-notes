{
  "name": "reading-notes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:docs-index": "node scripts/build-docs-index.mjs",
    "new:note": "./scripts/newnote.sh",
    "deploy": "npm run build:docs-index && npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@carbon/react": "^1.90.0",
    "@carbon/styles": "^1.89.0",
    "react-markdown": "^9.1.0",
    "react-router-dom": "^6.30.1",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  }
}