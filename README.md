# Reading Notes v2

This is a fresh start of the reading-notes project. The full v1 remains on the branch `archive/v1` (tag: `v1-final`).

Next steps you might take:
- add your new template structure
- inject the content you want to keep into fresh markdown files
- set up any build/dev tooling you prefer

Notes:
- The previous markdown corpus lives intact on `archive/v1`.
- This main branch begins with a clean history.
